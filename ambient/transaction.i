/**
 * Returns a LOGICAL value that indicates whether a transaction is currently active.
 *
 * Syntax
 *
 * `TRANSACTION`
 *
 * @returns Returns TRUE if a transaction is currently active, otherwise FALSE.
 */
FUNCTION TRANSACTION RETURNS LOGICAL (
  ) FORWARD.
